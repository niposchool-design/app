# Log de Correcoes - Nipo School

**Ultima atualizacao:** 2026-03-02

Este log registra correcoes relevantes no estado atual.

---

## Correcoes recentes

1. Padronizacao de docs para App Router e `proxy.ts`.
2. Consolidacao de docs de migrations como sequenciais.
3. Atualizacao de labels academicas em migrations (`055d` + `056`).

---

## Regra de uso

- Registrar apenas correcoes verificadas no codigo atual.
- Nao manter entrada baseada em estrutura antiga (`src/features/*`, `middleware.ts` legado, React Router legado).
